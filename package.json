{
  "name": "ice-breaker-root",
  "version": "1.0.0",
  "scripts": {
    "install-modules": "npm install --prefix client && npm install --prefix server",
    "build-client": "npm run build --prefix client",
    "build": "npm run install-modules && npm run build-client",
    "start": "node server/index.js"
  },
  "engines": {
    "node": "18.x"
  }
}